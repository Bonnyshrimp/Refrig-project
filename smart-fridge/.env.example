# คัดลอกไฟล์นี้เป็นชื่อ .env แล้วใส่ค่าจริงจาก Supabase Dashboard
# (Settings → API Keys / Data API) — ไฟล์ .env ถูก gitignore ไว้ ไม่ขึ้น git
EXPO_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOi...
